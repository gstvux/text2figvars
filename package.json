{
  "name": "paste2figvars",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && cp src/ui.html dist/ui.html && esbuild src/code.ts --bundle --outfile=dist/code.js",
    "watch": "mkdir -p dist && cp src/ui.html dist/ui.html && esbuild src/code.ts --bundle --outfile=dist/code.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.123.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
